# Arquivo .gitignore para projetos Node.js

# Logs
# Ignora arquivos de log que podem ser gerados durante a execução ou debug.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Dependências
# A pasta node_modules contém todos os pacotes instalados.
# Ela nunca deve ser enviada ao Git, pois pode ter milhares de arquivos
# e qualquer pessoa pode recriá-la usando 'npm install'.
node_modules/

# Arquivos de ambiente
# O arquivo .env contém variáveis sensíveis como senhas de banco de dados,
# chaves de API e segredos de JWT. É uma falha de segurança gravíssima enviá-lo.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Arquivos de sistema operacional
# Ignora arquivos de cache e configuração gerados por sistemas operacionais.
.DS_Store
Thumbs.db

# Diretórios de build e produção
# Se você fosse compilar seu código (ex: com TypeScript), ignoraria a pasta de saída.
/build
/dist

# Relatórios de testes
# Ignora relatórios de cobertura de testes.
/coverage

# Arquivos de editores de código
# Ignora configurações específicas de editores como VSCode.
.vscode/
